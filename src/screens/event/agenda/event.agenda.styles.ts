import StyleSheet from "react-native-extended-stylesheet";

export const styles = StyleSheet.create({
    agenda: {
        padding: 16
    },
    date: {
        fontWeight: "bold",
        fontSize: 22,
        marginBottom: 8
    }
});